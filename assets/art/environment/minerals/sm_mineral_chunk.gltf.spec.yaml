# Generated by tools/voxel-minerals/build_mineral_chunk.py -- regenerate rather
# than edit.
#
# A tapering crystal spike on a chunky base, carved from a 8x16x8 voxel
# lattice at 0.025 m per voxel (191 voxels kept), then scaled 4x so the
# chunk reads at a distance. Fist-sized collectible in source geometry, now
# roughly torso-sized: pivot at the centre of the bounding box, and
# rests_on_ground is false because it starts embedded in a wall and later
# flies through the air when collected -- it never sits on a floor.
#
# facing_direction is deliberately absent, the same reasoning
# sm_rubble01.gltf.spec.yaml gives: a crystal shard has no canonical front, so
# declaring one would put an unconfirmable INFO line in every PR comment. Colour
# is not part of this asset at all -- COLOR_0 is neutral AO only, and the three
# mineral hues live in external materials swapped in at runtime.
width: 0.6000
height: 1.3000
depth: 0.6000
poly_count_budget: 350
up_direction: "+Y"
root_node_name: "crystal_chunk"
collision_expected: false
navigation_expected: false
min_material_count: 1
rests_on_ground: false
