# Generated by tools/voxel-rubble/build_rubble.py -- regenerate rather than edit.
#
# 12x6x11 chunk carved from a 13x7x12 voxel lattice at 0.06 m per voxel
# (370 voxels kept). Floating debris for the zero-g mine: pivot at the
# centre of the bounding box so it tumbles about itself, which is also why
# rests_on_ground is false.
#
# facing_direction is deliberately absent. It is optional, and a rock has no
# front -- declaring one would put an unconfirmable INFO line in every PR
# comment asking a reviewer to verify a fact that does not exist. up_direction
# still matters and is still checked.
width: 0.7200
height: 0.3600
depth: 0.6600
poly_count_budget: 600
up_direction: "+Y"
root_node_name: "rubble"
collision_expected: false
navigation_expected: false
min_material_count: 1
rests_on_ground: false
